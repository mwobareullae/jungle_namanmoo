[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: tests that use full data catalogs or broad regression paths; excluded from PR fast CI
    live: tests that call external paid services and require explicit opt-in env vars
